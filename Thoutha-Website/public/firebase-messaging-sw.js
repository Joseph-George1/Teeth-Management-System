/* 
 * TEETH MANAGEMENT SYSTEM - FIREBASE CLOUD MESSAGING SERVICE WORKER
 * NOTE: This is a compiled file generated from firebase-messaging-sw.template.js
 * DO NOT EDIT THIS FILE DIRECTLY. YOUR CHANGES WILL BE OVERWRITTEN.
 * This file is automatically git-ignored.
 */

/* eslint-disable no-undef */
importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js');
importScripts('https://www.gstatic.com/firebasejs/10.14.1/firebase-messaging-compat.js');

firebase.initializeApp({
  apiKey: 'API_KEY_PLACEHOLDER',
  authDomain: 'AUTH_DOMAIN_PLACEHOLDER',
  projectId: 'PROJECT_ID_PLACEHOLDER',
  storageBucket: 'STORAGE_BUCKET_PLACEHOLDER',
  messagingSenderId: 'SENDER_ID_PLACEHOLDER',
  appId: 'APP_ID_PLACEHOLDER',
});

const messaging = firebase.messaging();

messaging.onBackgroundMessage((payload) => {
  console.log('[FCM SW] Background message received:', payload);

  if (payload.notification) {
    console.log('[FCM SW] Notification payload detected, SDK handles display automatically.');
    return;
  }

  const notificationTitle = payload.data?.title || 'حجز جديد - ثوثة';
  const notificationBody = payload.data?.body || 'لديك حجز جديد من مريض';

  const notificationOptions = {
    body: notificationBody,
    icon: '/ثوثة.png',
    badge: '/thoutha-48x48.png',
    dir: 'rtl',
    lang: 'ar',
    tag: 'thoutha-booking-' + Date.now(),
    renotify: true,
    data: {
      url: payload.data?.url || '/doctor-home',
      timestamp: Date.now(),
    },
    actions: [
      {
        action: 'open',
        title: 'عرض الحجز',
      },
    ],
  };

  return self.registration.showNotification(notificationTitle, notificationOptions);
});

self.addEventListener('notificationclick', (event) => {
  event.notification.close();
  const targetUrl = event.notification.data?.url || '/doctor-home';

  event.waitUntil(
    clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clientList) => {
      for (const client of clientList) {
        if (client.url.includes(self.location.origin) && 'focus' in client) {
          client.focus();
          client.navigate(targetUrl);
          return;
        }
      }
      return clients.openWindow(targetUrl);
    })
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});
