package com.spring.boot.graduationproject1.model;

/**
 * Enum for different user types in the notification system
 * Used across multiple entities to distinguish between PATIENT, DOCTOR, and ADMIN users
 */
public enum UserType {
    PATIENT,
    DOCTOR,
    ADMIN
}
